/**
 * Modulo de Math
 * @module dividir
 */
/**
 * Sirve para dividir un numero entre otro
 * @param {{x: number, y:number}} Obj Elementos que se dividiran
 * @returns {number} Division de x/y
 */
const dividir = ({ x, y }) => x / y;
export default dividir;
